import turtle

window = turtle.Screen()
tortuga = turtle.Turtle()

forward_value = 100
grades = 90

tortuga.forward(forward_value)
tortuga.right(grades)

tortuga.forward(forward_value)
tortuga.right(grades)

tortuga.forward(forward_value)
tortuga.right(grades)

tortuga.forward(forward_value)
tortuga.right(grades)

window.mainloop()
